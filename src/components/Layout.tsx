import { InputArea, OutputArea } from '.';

const Layout = () => {
	return (
		<div className="container">
			<div className="row">
				<InputArea />
				<OutputArea />
			</div>
		</div>
	);
};

export default Layout;
